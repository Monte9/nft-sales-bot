overwrite: true
schema:
  - ${DEAR_EARTH_HASURA_GRAPHQL_ENDPOINT}:
      headers:
        X-Hasura-Admin-Secret: ${DEAR_EARTH_HASURA_GRAPHQL_API_KEY}
documents: null
generates:
  src/graphql/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
